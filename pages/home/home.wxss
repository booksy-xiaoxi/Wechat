/* pages/home/home.wxss */
/* 父容器（如 .subject-item） */
.subject-item {
  position: relative; /* 确保子元素绝对定位相对于此容器 */
  width: 100%;
  height: 100%;
}

/* 上层图片样式 */
.overlay-image {
  position: absolute;
  width: 100rpx;
  height: 100rpx;
  bottom: 20rpx;
  right: 20rpx;
  z-index: 1;
}